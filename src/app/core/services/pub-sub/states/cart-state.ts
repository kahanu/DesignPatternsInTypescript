/**
 * This object state can hold any number of properties that can be passed
 * from the publisher to the observers.
 *
 * Currently it just holds the index of the selected dynamic cart component
 * to be displayed in the page.
 */
export class CartState {
  index = 0;
}
